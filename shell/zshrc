eval "$(/opt/homebrew/bin/brew shellenv)"
source "$HOME/dotfiles/shell/prompt.sh"

export CLICOLOR=1
export LSCOLORS=ExFxBxDxCxegedabagacad

. "$HOME/.cargo/env"

. "$HOME/.local/bin/env"

# Added by LM Studio CLI (lms)
export PATH="$PATH:/Users/ty/.lmstudio/bin"
# End of LM Studio CLI section

# Claude Code sandbox (coding-agent-sandbox submodule)
alias claude-box='CLAUDE_SANDBOX_IMAGE=ty60/claude-code-sandbox:latest "$HOME/dotfiles/coding-agent-sandbox/claude-sandbox.sh"'
